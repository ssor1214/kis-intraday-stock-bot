[project]
name = "kis-intraday-stock-bot"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = ["python-dotenv>=1.0", "requests>=2.31", "websocket-client>=1.8"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
