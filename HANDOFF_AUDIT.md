# 인수인계 검증 — 2026-09-06

대상: ssor1214/kis-intraday-stock-bot, main 커밋 dd3fba98d4894abd62cc48ef6c66afa6405147ca.

## 확보 방법

현재 작업 폴더는 비어 있었음. Git HTTPS helper 누락으로 clone 실패 후 GitHub 연결을 통해 위 커밋의 12개 파일을 로컬에 복사함. 이 폴더는 clone이 아닌 소스 스냅샷임.

## 확인 결과

- 저장소는 Public. 업로드 전 Private 여부 확인이 필요하며 이번 작업에서 업로드나 공개설정 변경은 하지 않음.
- metadata의 push/admin 표시는 실제 Contents Write 성공을 보장하지 않음. 쓰기 권한은 미검증.
- 구현: 기본 수량계산, Capital Fit, 일일 손실 기준 비교, 비용 차감 함수, 설정, 상태 열거형, 메모리 주문 저장.
- LiveBroker.submit은 항상 예외를 발생시킴.
- KIS OAuth/REST/WebSocket 연결, 실제 모의주문 및 체결조회는 없음.
- Safety Filter, 동적 자본구간/성과 방어, 총노출 및 종목 한도 강제, 전략/점수/트리거, 청산 관리, 거래일지, MAE/MFE, 수급/뉴스/세션 처리는 없음.
- 상태는 Enum만 있고 전이 검증 및 중복 주문 방지는 없음.
- 기존 테스트는 3개. 인수인계서의 10/10 PASS에 해당하는 코드는 이 커밋에 없음.

## 검증 방법과 한계

번들 Python으로 기존 테스트 함수 3개를 runpy로 로드하여 직접 실행: 3/3 PASS. pytest 미설치로 pytest 실행 자체는 실패했으며, 이 결과를 pytest 통과로 표기하지 않음. 테스트들은 fixture 없는 단순 assert 함수임.

이 결과는 기본 계산만 확인하며 봇 운용 가능성, 수익성, KIS 실연결을 입증하지 않음. 실제 비밀값으로 보이는 내용은 확보한 12개 파일에서 발견되지 않았으나 전체 Git 이력 secret scan은 수행하지 않음. .env는 ignore됨.

## 후속 개발 기준

기존 V0.2 원본이 별도로 존재하는지 확인하여 중복 재구현을 피할 것. 없다면 이 스냅샷을 최소 골격으로 간주하고 공식 KIS 문서에 맞춘 모의 인증/시세 수집부터 구현할 것.

수량 계산에 비용 포함 위험, 기존 보유/미체결 주문 예약금, 동적 총노출과 종목 한도를 통합해야 함. 설정의 환경변수는 현재 import 시점에 평가되며 .env 자동 로딩은 없음. 비용 함수에 실제 체결가를 넣을 때 이미 반영된 슬리피지를 중복 차감하지 않도록 예상 비용과 실현 손익을 분리해야 함.

## 2026-09-06 구현 추가

인수인계서의 연구 코어를 `safety.py`, `exposure.py`, `strategy.py`, `exits.py`, `journal.py`로 추가했다. 이들은 데이터 구조와 정책 계산을 제공하며 KIS 실시간 데이터 공급자를 가장하지 않는다. 실제 API 연결, 뉴스/수급 공급원, 세션별 데이터 매핑, 장중 리플레이 검증은 다음 단계다.
